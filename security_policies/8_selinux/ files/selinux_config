# Этот файл управляет состоянием SELinux на системе
# SELINUX= может принимать одно из значений:
#   enforcing  - политика SELinux включена и применяется
#   permissive - политика включена, но только выводит предупреждения
#   disabled   - SELinux отключён
SELINUX=enforcing

# SELINUXTYPE= может принимать одно из значений:
#   targeted - защищаются только целевые процессы (по умолчанию)
#   minimum  - минимальная политика
#   mls      - мультиуровневая безопасность
SELINUXTYPE=targeted